# Copy this template to .env and fill the actual credentials for your SQL Server deployments.
# The Liquibase property files in Configuration Files rely on the LIQUIBASE_USER/PASSWORD values below.
LIQUIBASE_USER=sa
LIQUIBASE_PASSWORD=Admin123!

# Connection hints for each SQL Server profile (the ports match the gescomph-api GESCOMPH-DB compose):
# develop -> jdbc:sqlserver://localhost:1438;databaseName=gescomph_develop
# qa      -> jdbc:sqlserver://localhost:1436;databaseName=gescomph_qa
# staging -> jdbc:sqlserver://localhost:1437;databaseName=gescomph_staging
# main    -> jdbc:sqlserver://localhost:1435;databaseName=gescomph_main
